{
    "name": "unity",
  	"version": "0.0.1",
  	"private": true,
    "devDependencies": {
        "gulp-sass": "latest",
        "gulp-rename": "latest",
        "gulp-uglify": "latest"
    },
    "scripts": {
        "preinstall": "npm install -g gulp"
    }
}
